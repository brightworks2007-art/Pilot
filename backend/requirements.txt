fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
# Unpinned from an exact old version -- that version locked in an old
# `realtime` sub-dependency whose websockets requirement conflicted with
# google-genai's. Minimum version bound instead, so pip resolves a
# `realtime` release with a compatible (newer) websockets range.
supabase>=2.8.0
pydantic==2.7.4
pydantic-settings==2.3.4
python-docx==1.1.2
openpyxl==3.1.5
python-pptx==0.6.23
pypdf==4.2.0
reportlab==4.2.0
google-genai>=0.7.0
python-dotenv==1.0.1
